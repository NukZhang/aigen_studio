# UI Prototype 要求（最小可用）

允许的 Prototype 形态（满足其一即可）：
1) 静态 HTML（iframe 预览）
2) Vue 静态页面（本地 dev 预览）
3) 截图/图片（用于确认布局与主流程）
4) 外链 URL（必须记录到 Preview_Contract）

必须包含：
- 入口页（主流程起点）
- 至少 2 个核心路径的页面/状态
- 空态/错误态至少 1 个示例（文本说明也可）
